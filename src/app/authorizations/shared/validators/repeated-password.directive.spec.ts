import { RepeatedPasswordValidator } from './repeated-password.validator';

describe('RepeatedPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatedPasswordValidator();
    expect(directive).toBeTruthy();
  });
});
